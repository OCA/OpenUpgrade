<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- don't touch admin's fields -->
    <record id="user_admin" position="replace" />
    <!-- don't touch root's fields -->
    <record id="user_root" position="replace" />
</odoo>
