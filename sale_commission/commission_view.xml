<?xml version="1.0"?>
<terp>
<data>



	<record model="ir.ui.view" id="view_commission_month_form">
		<field name="name">commission.month.form</field>
		<field name="model">report.commission.month</field>
		<field name="type">form</field>
		<field name="arch" type="xml">
			<form string="sales agent commission">
				<field name="name" select="1"/>
				<field name="sono" select="1"/>
				<field name="productname" select="1"/>
				<field name="product_quantity" select="1"/>
				<field name="invno" select="1"/>
				<field name="in_date"/>
				<field name="inv_total" select="1"/>
				<field name="state"/>
				<field name="comrate" select="1"/>
				<field name="commission" select="1"/>
				<field name="pdate"/>
			</form>
		</field>
	</record>


	<record model="ir.ui.view" id="view_commission_month_tree">
		<field name="name">commission.month.form.tree</field>
		<field name="model">report.commission.month</field>
		<field name="type">tree</field>
		<field name="arch" type="xml">
			<tree string="sales agent commission">

				<field name="name" select="1"/>
				<field name="sono" select="1"/>
				<field name="productname" select="1"/>
				<field name="product_quantity" select="1"/>
				<field name="invno" select="1"/>
				<field name="in_date" select="1"/>
				<field name="inv_total" select="1"/>
				<field name="state" select="1"/>
				<field name="comrate" select="1"/>
				<field name="commission" select="1"/>
				<field name="pdate" select="1"/>
				</tree>
		</field>
	</record>
	<record model="ir.actions.act_window" id="action_sales agent commission_tree_month">
		<field name="name">commission.month.form.tree</field>
		<field name="res_model">report.commission.month</field>
		<field name="view_type">form</field>
		<field name="view_mode">tree</field>
		<field name="domain">[('in_date','ilike',time.strftime('%Y-%m'))]</field>

	</record>
	<menuitem name="Sales Management/Commissions/Reporting/This Month/All Commissions " action="action_sales agent commission_tree_month" id="menu_commission_tree_month"/>

	<record model="ir.actions.act_window" id="action_sales agent commission_tree_open">
		<field name="name">commission.month.form.tree</field>
		<field name="res_model">report.commission.month</field>
		<field name="view_type">form</field>
		<field name="view_mode">tree</field>
		<field name="domain">[('state','=','open'),('in_date','ilike',time.strftime('%Y-%m'))]</field>

	</record>
	<menuitem name="Sales Management/Commissions/Reporting/This Month/Opened Commissions " action="action_sales agent commission_tree_open" id="menu_commission_tree_open"/>

	<record model="ir.actions.act_window" id="action_sales agent commission_tree_paid">
		<field name="name">commission.month.form.tree</field>
		<field name="res_model">report.commission.month</field>
		<field name="view_type">form</field>
		<field name="view_mode">tree</field>
		<field name="domain">[('state','=','paid'),('in_date','ilike',time.strftime('%Y-%m'))]</field>

	</record>
	<menuitem name="Sales Management/Commissions/Reporting/This Month/Paid Commissions" action="action_sales agent commission_tree_paid" id="menu_commission_tree_paid"/>



	<record model="ir.ui.view" id="view_commission_all_form">
		<field name="name">commission.all.form</field>
		<field name="model">report.commission.month</field>
		<field name="type">form</field>
		<field name="arch" type="xml">
			<form string="sales agent all commission">
				<field name="name" select="1"/>
				<field name="sono" select="1"/>
				<field name="productname" select="1"/>
				<field name="product_quantity" select="1"/>
				<field name="invno" select="1"/>
				<field name="in_date" select="1"/>
				<field name="inv_total" select="1"/>
				<field name="state" select="1"/>
				<field name="comrate" select="1"/>
				<field name="commission" select="1"/>
				<field name="pdate" select="1"/>
			</form>
		</field>
	</record>


	<record model="ir.ui.view" id="view_commission_all_tree">
		<field name="name">commission.all.form.tree</field>
		<field name="model">report.commission.month</field>
		<field name="type">tree</field>
		<field name="arch" type="xml">
			<tree string="sales agent all commission">

				<field name="name" select="1"/>
				<field name="sono" select="1"/>
				<field name="productname" select="1"/>
				<field name="product_quantity" select="1"/>
				<field name="invno" select="1"/>
				<field name="in_date" select="1"/>
				<field name="inv_total" select="1"/>
				<field name="state" select="1"/>
				<field name="comrate" select="1"/>
				<field name="commission" select="1"/>
				<field name="pdate" select="1"/>
				</tree>
		</field>
	</record>
	<record model="ir.actions.act_window" id="action_sales agent commission_tree_all">
		<field name="name">commission.all.form.tree</field>
		<field name="res_model">report.commission.month</field>
		<field name="view_type">form</field>
		<field name="view_mode">tree</field>
	</record>
<menuitem name="Sales Management/Commissions/Reporting/All Commissions/All Commissions" action="action_sales agent commission_tree_all" id="menu_commission_tree_all"/>

<record model="ir.actions.act_window" id="action_sales agent commission_tree_all_open">
		<field name="name">commission.all.form.tree</field>
		<field name="res_model">report.commission.month</field>
		<field name="view_type">form</field>
		<field name="view_mode">tree</field>
		<field name="domain">[('state','=','open')]</field>
	</record>
<menuitem name="Sales Management/Commissions/Reporting/All Commissions/Opnend Commissions" action="action_sales agent commission_tree_all_open" id="menu_commission_tree_all_open"/>

<record model="ir.actions.act_window" id="action_sales agent commission_tree_all_paid">
		<field name="name">commission.all.form.tree</field>
		<field name="res_model">report.commission.month</field>
		<field name="view_type">form</field>
		<field name="view_mode">tree</field>
		<field name="domain">[('state','=','paid')]</field>
	</record>
<menuitem name="Sales Management/Commissions/Reporting/All Commissions/Paid Commissions" action="action_sales agent commission_tree_all_paid" id="menu_commission_tree_all_paid"/>

</data>
</terp>
