<?xml version="1.0"?>
    <openerp>
        <data>

            <!--
            =======================================================
            COUNTRY EXTENSIONS
            =======================================================
            -->
            <record id="view_customer_file_form_inherit" model="ir.ui.view">
                <field name="name">dm.customers.file.inherit</field>
                <field name="model">dm.customers_file</field>
                <field name="type">form</field>
                <field name="inherit_id" ref="base.dm.customers.file.form"/>
                <field name="arch" type="xml">
                    <field name="source_id" position="after">
                        <group colspan="4" attrs="{'invisible':[('source_id','!=','CRM Leads')]}"
                        <field name="case_ids" select="1"/>
                    </field>
                </field>
            </record>
        </data>
    </openep>

